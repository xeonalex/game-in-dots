import React, {Component} from 'react';
import GameCell from "components/GameField/GameCell";
import {random} from 'lodash'

class GameField extends Component {
    state= {
        settings: 25,
        arr: new Array(25)
    };

    activateRandomCell() {
        return random(0, 25);
    }

    onGameCellClick(){
        return true;
    }

    render() {
        let {
            state: {arr},
            onGameCellClick
        } = this;

        return (
            <div>
                {
                    arr.map((cell)=>
                        return <GameCell handleClick={onGameCellClick}/>
                    )
                }
            </div>
        );
    }
}

export default GameField;