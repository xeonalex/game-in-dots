import React from 'react';
import 'styles.scss'

const GameCell = ({handleClick}) => {
    return (
        <div className={'game-field__cell'} onClick={handleClick}/>
    );
};

export default GameCell;
